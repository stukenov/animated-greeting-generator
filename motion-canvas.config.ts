import {defineConfig} from '@motion-canvas/vite-plugin';

export default defineConfig({
  output: {
    name: 'congratulation',
    resolution: {
      width: 1920,
      height: 1080,
    },
    fps: 60,
  },
}); 